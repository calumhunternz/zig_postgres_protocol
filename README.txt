This is a WIP implementation of a postgres protocol with native zig.

Current functionality as of writing this is the client can connect to a postgres db
and establish a SASL authenticated connection that is ready to query the databases.
